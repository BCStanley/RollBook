"""Narrow-down system.

Ranks and formats matching's candidates into a review-ready CSV. Ported
from `process_candidate.py` in the legacy `case_locator` repo.

Implemented in Phase 7 of the project plan.
"""
