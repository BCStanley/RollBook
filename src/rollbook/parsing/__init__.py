"""Line parsing system.

Splits each cleaned index line into structured fields: party names,
citation fragments, page numbers. Ported from the line-splitting logic
in `helpers.py` in the legacy `case_locator` repo.

Implemented in Phase 3 of the project plan.
"""
