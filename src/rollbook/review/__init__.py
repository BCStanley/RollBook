"""Review system.

Ingests a hand-reviewed CSV (KEEP/DROP/MAYBE per candidate, edited by the
researcher — kept as a CSV + vim workflow for now, per the project plan)
back into a set of confirmed matches. Ported from `build_index.py` in the
legacy `case_locator` repo.

Implemented in Phase 7 of the project plan.
"""
