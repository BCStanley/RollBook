"""Candidate database system.

Builds and queries the SQLite database of candidate cases drawn from
source authority lists (.txt files). Ported from `database_utils.py` and
`build_whole_database.py` in the legacy `case_locator` repo.

Implemented in Phase 5 of the project plan.
"""
